# ── 後端環境變數設定（複製為 .env 後修改）
# 不要把 .env 上傳到 GitHub！

# 允許的前端來源（CORS）
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

# 模型路徑（預設 model.pkl）
MODEL_PATH=model.pkl

# 資料集路徑（訓練時使用）
DATA_PATH=nigeria_sme_loans_full_sample.csv

# 台灣央行基準利率（可依市場更新）
BASE_RATE=0.02000

# 環境（development / production）
ENVIRONMENT=development
